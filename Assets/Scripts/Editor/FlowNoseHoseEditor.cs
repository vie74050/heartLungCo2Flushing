using UnityEditor;
using UnityEngine;
/*
#if UNITY_EDITOR
    [CustomEditor(typeof(FlowNodeHose))]
    public class FlowNodeHoseEditor : Editor
    {
        public override void OnInspectorGUI()
        {
            DrawDefaultInspector();

            FlowNodeHose hose = (FlowNodeHose)target;
            if (GUILayout.Button("Update Hose Mesh & Collider"))
            {
                hose.UpdateHoseLineRenderer();
            }
        }
    }
#endif
*/